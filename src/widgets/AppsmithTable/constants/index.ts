export enum ItemSize {
  "xs" = "xs",
  "sm" = "sm",
  "md" = "md",
  "lg" = "lg",
}

export enum PinDirection {
  left = "left",
  right = "right",
}

export enum HTTP_METHODS {
  "GET" = "GET",
  "POST" = "POST",
  "PUT" = "PUT",
  "DELETE" = "DELETE",
  "PATCH" = "PATCH",
}

export enum ColumnType {
  TEXT = "text",
  NUMBER = "number",
  ENUM = "enum",
  BOOL = "bool",
  ID = "id",
  DATE = "date",
  CURRENCY = "currency",
  PHONE = "phone",
}
